version: '3.3'

services:
   build:
      image: zenika/alpine-maven
      volumes:
        - $PWD:/app
      build: 
         context: .
   test:
      image: zenika/alpine-maven
      volumes:
         - $PWD:/app
      working_dir: /app
      command: mvn surefire:test
      
